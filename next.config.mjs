/**
 *  @type {import('next').NextConfig}  
*/
const nextConfig = {
    basePath: '/todolist', // Replace with your GitHub repository name
    assetPrefix: '/todolist/',
    // output:'export',
    // distDir:'dist',
    // images:{
    //     unoptimized: true,
    // }
};

export default nextConfig;
